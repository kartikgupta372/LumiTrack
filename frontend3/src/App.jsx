// frontend/ is the canonical merged MVP. Keep this historical entrypoint usable
// without allowing the 2D prototype to drift from the unified application.
export { default } from '../../frontend/src/App.jsx';
